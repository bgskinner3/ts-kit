// ===========================
// ===========================
// COLOR TYPES / AUDITOR
// ===========================
// ===========================
type TRGB = readonly [r: number, g: number, b: number];
type TRGBTuple = [number, number, number];
type TCssRGB = `rgb(${number}, ${number}, ${number})`;

export type { TRGB, TRGBTuple, TCssRGB };
